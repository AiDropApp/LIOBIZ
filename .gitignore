# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# contact messages + production CMS (server is source of truth — never commit)
/data/contact-messages.json
/data/media-center.json
/data/site-content.json
/data/snapshots/
/data/filesir-local-map.json
/data/filesir-migrate-report.json
/data/liobiz.db
/data/liobiz.db-*
/data/backups/
/data/pre-deploy-*/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*
!.env.example

# vercel
.vercel

# user uploads
/public/uploads/

# large media kept locally (copy into public/videos for deploy)
/header.mp4

# Full media library lives on server/local disk (too large for GitHub).
# Lightweight thumbs are committed for portfolio fast-load.
/public/media/**
!/public/media/.thumbs/
!/public/media/.thumbs/**

# local sync archives
/liobiz-*.tar
/liobiz-*.tgz
../liobiz-media.tar
../liobiz-code-sync.tgz
../liobiz-thumbs.tgz

# typescript
*.tsbuildinfo
next-env.d.ts

# local debug dumps
err.txt
out.txt
listfiles.txt
trace.txt
trace-filtered.txt
tsc-list.txt
tsc-resolve.txt
ts-trace.txt

# deploy secrets / certs (never commit)
.deploy/

# local deploy artifacts
deploy-full.tar
next-build.tar

# test artifacts
/test-results/
/docs/playwright-report/
/playwright-report/
